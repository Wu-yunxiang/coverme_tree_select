# CoverMe Tree Select

基于树搜索策略的代码覆盖率估算工具。

### 1. 快速构建
```bash
rm -rf build && cmake -S . -B build && cmake --build build
```

### 2. 运行测试
```bash
python3 src/coverage_algorithm.py -n 5 --stepSize 300
```

### 3. 查看结果
测试生成的有效输入将保存在 `output/effective_input.txt` 中。

### 4. 项目演进
由于阶段二种子很少，种子成功率低且锁死上限，所以决定把两个阶段融合
首先对于每个种子按照 basinhopping编号，powell编号，powell内部迭代编号，函数运行编号的缩进格式输出，观察input的变化值和所有未覆盖点的满足数量和距离变化。期望得到找到覆盖的距离的变化情况，以及失败的距离变化情况，根据经验做出判定性的决策A快速排除错误的初始种子 （通过不同种类的扰动覆盖成败的差异情况，多臂老虎机等）
若input变化明显，（为了充分利用必须进行的函数调用）则每次运行待测函数维护一个遍历的集合，对于每个未覆盖目标，二分得到满足的条件和距离，根据评估函数（满足条件数量，总条件数量，距离，之前种子的质量，失败次数）决定当前种子（是否优质）是否有必要作为该目标的初始。（变相于把相似度高的归为类似的种子，且对于每个目标进行了定制）
若变化适中，则间隔k(层次)进行一次。若很不明显，则只在初始进行一次。
将距离函数改为满足条件数量和距离的阶梯函数。删掉梯度队列逻辑？
可以考虑对已经解决的解进行不同种类的融合策略，代替随机生成？

如果target一直没有满足，则对于非target优化过程并没有指向性的意义，但是
过程中仍然可能出现良好的结果。但貌似这些良好结果起不到应有得作用，因为哪怕是最后的时候，平均生成100个随机种子至少有一个可以作为有意思的解，但是如果是迭代是否不止一百个，开销并不在寻找刚刚达标的解上，而是解的优良性质上。那就意味着不如随机生成？唯一的价值就是找到的种子的优质程度很难出现在随机生成中

有几个猜测方向，当自变量都很大的时候，继续迭代（由于迭代步长很小）不会出现优质的解了。这个方向可以查看出现新的覆盖时的相比于上一次的自变量变化值和自变量的值
当前seed出现了很多连续0，不会再出现非0了
根本目的是缩小函数调用次数
查找成功的iter,根据优化流程特性减，足够多的都可以被高效的随机替代
可以分析变量变化的策略？
如果一个目标按照之前标准取得优质种子的难度
相比于进行优化（进行取舍策略后的总体难度）的难度低，则不断精确标准，如果过于严格则二分回退,func_run_count的阈值和变量维度有关，可能是线性关系

